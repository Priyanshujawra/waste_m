import React from "react";
import "./gloabLoader.css"; // Assuming the CSS styles are moved to a separate file

const Loader = () => {
  return <div className="loader"></div>;
};

export default Loader;
